{
  "name": "globs-world-explorer",
  "version": "0.1.0",
  "private": true,
  "description": "GlobS — World Explorer: a lightweight, source-transparent web portal for discovering current information from around the world.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@linkurious/leaflet-heat": "^0.2.6",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.20",
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
